const HelpdeskAdapter = require('./helpdesk');
const CRMAdapter = require('./crm');
const POSAdapter = require('./pos');
const AccountAdapter = require('./account');

module.exports = {
  HelpdeskAdapter,
  CRMAdapter,
  POSAdapter,
  AccountAdapter
};
